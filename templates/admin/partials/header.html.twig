<nav class="navbar navbar-expand-lg navbar-dark bg-primary container-fluid sticky-top" style="margin-bottom: 10px;">
    <a class="navbar-brand ml-5" href="{{ path('home') }}">Mini airbnb</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link"  href="{{ path('admin_dashboard') }}">
                   <i class="fas fa-chart-line "></i> Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_adverts_index') }}">
                    <i class="fas fa-hotel "></i> Annonces
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_bookings_index') }}">
                    <i class="fas fa-folder"></i> Réservations
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_comments_index') }}">
                    <i class="fas fa-comments"></i>Commentaires
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">
                    <i class="fas fa-users"></i>Utilisateurs
                </a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto mr-5">
            <I class="nav-item">
                <a href="{{ path('home') }}" target="_blank" class="nav-link">
                    <i class="fas fa-eye"></i> voir le site
                </a>
            </I>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" title="Ouvrir le menu">
                    {% if app.user.avatarUrl %}
                        <img class="nav-profile-img rounded-circle avatar-small" src="{{ app.user.avatarUrl }}" title="Ouvrir le menu" alt="avatar de {{ app.user.fullName }}" style="width: auto; height: auto">
                    {% else %}
                        <img class="nav-profile-img rounded-circle avatar-small" src="{{ '/images/avatar_profile.png' }}" style="width: auto; height: auto" title="Ouvrir le menu" alt="avatar de {{ app.user.fullName }}">
                    {% endif %} {{ app.user.fullName }}
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="{{ path('app_admin_logout') }}" onclick="return confirm(
                         'Vous êtes sur le point de vous déconnecter.\n'+
                         'Êtes-vous sûr de vouloir continuer ?')">
                        Déconnexion
                    </a>
                </div>
            </li>
        </ul>
    </div>
</nav>
{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }} text-center">
            {% for message in messages %}
                {{ message | raw }}
            {% endfor %}
        </div>
    </div>
{% endfor %}