{{ form_start(form) }}
    <div class="alert alert-light">
        <h2 class="text-center">Commencez par choisir vos dates</h2>
        <div class="row">
            <div class="col">
                {{ form_row(form.startDate) }}
            </div>
            <div class="col">
                {{ form_row(form.endDate) }}
            </div>
        </div>
        <h4>
            Montant du séjour: <span id="amount">... </span> &euro;
        </h4>
        <span id="days">0</span> nuits à {{advert.price| number_format(2, ',', ' ') }} &euro; par nuit
        <h2 class="text-center">Vous avez une démande particulière ?</h2>
        {{ form_row(form.comment) }}
        <button type="submit" class="btn btn-secondary ">{{ button_label|default('Confirmer la réservation') }}</button>
    </div>
{{ form_end(form) }}
